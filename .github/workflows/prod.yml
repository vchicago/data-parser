name: Build and Deploy Image

on:
  push:
    branches: [ "master" ]
    
jobs:
  build:
    runs-on: ubuntu-latest
    name: Build image

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build . -t kzdv/flight-parser:${GITHUB_SHA::6}
      - name: Upload SHA tagged image
        run: docker push kzdv/flight-parser:${GITHUB_SHA::6}
      - name: Set image to latest tag
        run: docker tag kzdv/flight-parser:${GITHUB_SHA::6} kzdv/flight-parser:latest
      - name: Push latest Image
        run: docker push kzdv/flight-parser:latest
      - name: Deploy image to cluster
        uses: actions-hub/kubectl@master
        env:
          KUBE_CONFIG: ${{ secrets.KUBECTL }}
        with:
          args: set image deployment/flight-parser flight-parser=kzdv/flight-parser:${GITHUB_SHA::6} -n prod
